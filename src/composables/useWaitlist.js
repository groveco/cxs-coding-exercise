import { getWaitlist, createWaitlistItem } from "@/services/waitlist";

/** @typedef WaitlistComposition - TO BE IMPLEMENTED */

/**
 * When fully implemented, this composable should return a "composition" 
 * consisting of any values and methods needed to
 * 1) determine what products the customer currently has on their waitlist and
 * 2) to update the waitlist.
 * 
 * @see https://vuejs.org/guide/reusability/composables.html#what-is-a-composable
 * @returns {WaitlistComposition}
 */
const useWaitlist = () => {
  return {
    // TO BE IMPLEMENTED
  };
};

export default useWaitlist;